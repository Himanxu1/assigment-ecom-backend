# TypeScript and the @types/* packages live in devDependencies, but the build
# needs them. Hosts that set NODE_ENV=production would otherwise skip them and
# fail with "Could not find a declaration file for module 'express'".
include=dev
